#include "main.h"
/**
*_abs(int): function to print the absolute value of an integer
*
*/
int _abs(int c)
{
if (c < 0)
{
int absvalue = -c;
_putchar("absvalue % 10" + '0');
}
else if (c >= 0)
{
_putchar("c % 10" + '0');
}
}
